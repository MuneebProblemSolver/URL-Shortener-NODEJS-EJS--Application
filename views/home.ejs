<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home Page</title>
</head>
<style>
    body{
        margin: 20px;
        font-family:Cambria, Cochin, Georgia, Times, 'Times New Roman', serif;
    }
</style>
<body>
    <h1>URL Shortner</h1>
    <% if (locals.id) { %>
     <p> URL Genrated: http://localhost:8001/url/<%= id %></p>
    <% } %>
    <div>
        <form action="/url" method="post">
            <label>Enter Your Origonal URL</label>
            <input type="text" name="url" id="" placeholder="hhtps://example.com">
            <button type="submit"> Genrate </button>
        </form>
    </div>
    <div>
        <% if (locals.urls) { %>
         <table>
            <thead>
                <th>SNo.</th>
                <th>Short ID</th>
                <th>Redirect URL</th>
                <th>
                    No. of Clicks
                </th>
            </thead>
            <tbody>
            <% urls.forEach((url,index) => { %>
                <tr>
             <td><%= index + 1  %></td>
             <td> <%= url.shortId %></td>
             <td><%= url.redirectURL  %></td>
            <td><%= url.visitHistory.length  %></td>
             <% }
            ) %>
            </tr>
            </tbody>
         </table>
        <% } %>
    </div>
</body>
</html>